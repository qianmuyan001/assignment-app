# Apple 2.1.2 正式验收手册

状态：**已开始，独立部分已执行；完整正式验收未通过**（2026-09-08）。用户已授权“开始验收”，并要求先完成可独立执行部分。本轮冻结 SHA、真实结果和原始证据索引见 [独立验收结果](../phase-reports/apple-2.1.2-acceptance-results.md)。下面仅更新实际覆盖的行；混合场景拆分注明范围，未执行的人工项目不预填通过。

“开始验收”不授权 push、合并 main、tag、GitHub Release、公证上传或 TestFlight 分发。涉及这些操作仍需各自明确授权；签名账号和用户测试参与者也须事先具备。

## 候选冻结与记录

| 项目 | 状态 | 执行时填写 |
| --- | --- | --- |
| 确认待验收分支、完整 SHA、工作树 clean | 通过 | d8407fc9d3adfb72efff133255e3e79076ab38e1，2026-09-08 |
| 独立验收工作树与专属构建缓存 | 通过 | apple-2.1.2-acceptance 独立分支及缓存，见结果报告 |
| 检查根版本协调补丁已获批准并通过共享版本门禁 | 阻断 | 实际执行失败：根 2.1.0 / Apple 2.1.2；协调补丁未应用 |
| 从冻结提交重新构建并核对 2.1.2 / build 2 / Schema v4 | 通过（内部身份） | Debug/Release arm64；正式身份分发未执行 |
| 从冻结提交执行 iPad/Catalyst 全量单元及 UI 自动测试 | 通过 | 181/181、181/181、6/6，均零失败零跳过；完整数字见结果报告 |
| 正式数据全程只读指纹前后比较 | 通过 | 原始 before/final 内容完全一致；正式数据未建立 SQLite 连接 |

先用 `native/apple/scripts/protected-data-fingerprint.py before.json` 建立正式数据快照；结束和任何失败退出时用 `after.json --compare before.json` 比较。该脚本只读取文件，不连接正式 SQLite。只要正式数据出现新文件或任何指纹变化，立即阻断验收，保存证据；不要为让比较通过而恢复时间戳、覆盖文件或更新 before。

## 系统通知矩阵

全部使用稳定内部身份及合成任务。记录系统、应用状态、权限、请求 UUID、触发时间、实际收到时间、点击结果；证据遮盖敏感路径。开发路由单元测试不是以下项目通过的证据。

| 场景 | 状态 | 期望 |
| --- | --- | --- |
| 首次请求允许 / 拒绝；设置页状态与错误反馈 | 部分执行 | 实际请求后显示 Allowed；拒绝及错误反馈完整矩阵未执行 |
| 固定时间、截止前相对提醒，实际系统横幅与声音 | 未执行 | 每条一次，时间正确 |
| 修改截止时间或提醒后旧请求取消 | 未执行 | 只收到更新后的提醒 |
| 完成、删除、禁用后的取消 | 未执行 | 无新横幅，过期已交付项按任务生命周期清理 |
| 应用运行且无弹窗，点击通知 | 未执行 | 打开对应任务一次 |
| 应用运行且正在编辑另一任务 | 未执行 | 保留编辑内容，关闭当前编辑器后定位 |
| 应用完全退出，点击横幅冷启动 | 未执行 | launch delegate 收到响应，数据库就绪后定位一次 |
| 首次引导尚未结束 / 数据库暂时不可用 | 未执行 | 保留响应，恢复/引导结束后处理，无错误数据回退 |
| 目标任务被删除、不存在、已完成 | 未执行 | 已删除/不存在提示；已完成任务可定位 |
| 系统设置撤销/恢复权限，再激活或重启 | 未执行 | 核对并取消/恢复待调度请求，无重复 |
| 锁屏、勿扰、摘要投递、睡眠唤醒 | 未执行 | 分清系统策略与应用缺陷；记录真实结果 |

## 桌面窗口、键盘与无障碍

| 页面 / 矩阵 | 状态 | 核对内容 |
| --- | --- | --- |
| 任务列表/编辑、课程表、考试、日历、备份、设置/About | 未执行 | 中文、英文及长标题/描述/课程名 |
| 宽窗口、约 520pt 窄窗口、多窗口 | 部分执行 | 任务页实际 520×768 和 1100×800；完整页面/多窗口人工矩阵未执行 |
| 展开与图标侧栏、退出重启后的偏好 | 部分通过 | 内部应用图标模式、退出及实际删除重装后偏好保留；完整可访问性矩阵未执行 |
| 搜索展开、清空、Escape、外部关闭 | 部分执行 | 执行展开/输入/Escape/Command-F/关闭；标题恢复和焦点完整矩阵未执行 |
| Command-N/F/R、Tab/Shift-Tab、表单滚动与关闭 | 未执行 | 仅当前窗口响应；编辑/警告/引导期间不穿透 |
| VoiceOver、最大字号、增大对比度、减少透明度 | 未执行 | 语义/朗读顺序/焦点可达；无内容被裁断 |
| Reduce Motion、键盘快速切换 | 未执行 | 无不必要位移动画或焦点丢失 |
| 加载、空状态、数据库/附件/备份错误 | 部分执行 | 实际故障迁移明确报错且回滚；其余完整人工矩阵未执行 |

## 安装、升级、卸载与恢复

使用 `upgrade-rehearsal.py`，固定身份 `com.qianmuyan.assignmentapp.internal.upgrade`。不同版本必须具有同一内部 ID；随机 `rcsmoke` 包不能用于连续升级结论。合成 v2+WAL 由 prepare 生成，v3/v4 的附件及关系由内部旧客户端创建。严禁从日常数据库直接演练。

| 场景 | 状态 | 证据要求 |
| --- | --- | --- |
| 全新安装、首次启动和引导 | 部分通过 | 旧 2.1.0 固定内部空容器启动/引导及新 2.1.2 随机空容器启动；完整 2.1.2 引导人工矩阵未执行 |
| 稳定内部旧版覆盖为 2.1.2 | 通过（内部环境） | 7086f6f + 稳定身份隔离补丁 → d8407fc；实际加载并逐字段比较 |
| v2、v3 → v4 | 部分通过 | v2+WAL 实际应用迁移通过；v3 自动回归通过，独立旧应用安装场景未执行 |
| 带已提交 WAL 的旧库 | 通过（合成内部数据） | 非空 WAL receipt、应用实际迁移、任务内容/quick_check/外键检查 |
| 附件、描述、截止/完成时间、课程/项目/标签/子任务/考试关系 | 部分通过 | 前述字段及非空关系/附件逐项相同；考试/课程安排行不在演练夹具中，未执行 |
| 迁移失败与备份恢复 | 部分通过 | 故障迁移回滚保留 v2/备份；恢复已知良好 v4 目录可用；UI 导入迁移备份未执行 |
| 退出后卸载并重新安装 | 通过（内部环境） | 实际删除本轮内部 app 后重装，任务/全部字段/附件/侧栏偏好保留 |
| 明确删除内部测试数据 | 未执行 | 单独确认路径和删除意图；停止全部连接后操作 |

## 分发与用户测试

| 项目 | 状态 | 条件 |
| --- | --- | --- |
| Developer ID Application 签名 | 未执行 | 有效身份、私钥本机可用、适用 provisioning |
| 正式稳定身份、Sandbox、Hardened Runtime、时间戳与严格验签 | 未执行 | Release arm64，嵌套代码从内向外签名 |
| 公证提交、Accepted、stapling | 未执行 | 明确上传授权、账号及 notarytool profile |
| 下载/隔离属性下 Gatekeeper 与 DMG 安装 | 未执行 | 最终签名公证包、真实测试 Mac |
| Team Spirit 品牌图标 | 未执行 | 用户提供有使用权的素材，资源构建及视觉检查 |
| Alpha 内部人工矩阵 | 未执行 | 指定参与者完成上述矩阵，所有阻断缺陷关闭 |
| 真实用户 Beta | 未执行 | 同意参加的学生、反馈窗口、可回滚备份 |
| TestFlight | 未执行 | 单独 App Store Connect 流程；非 Developer ID 的替代条件 |

Alpha/Beta 反馈只主动提供：包名与 SHA、版本/build、系统/机型、复现步骤、期望/实际结果、发生频率、脱敏截图、用户主动复制的诊断摘要。不要索取完整日常数据库、任务正文、凭据或个人沙箱路径。

验收人应逐项填写真实执行时间、结果、证据和缺陷编号；未满足条件仍保留“未执行”，不能用开发报告的自动通过结果预填。
